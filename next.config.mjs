/** @type {import('next').NextConfig} */
const nextConfig = {
  // Si tenías experimental.serverActions, elimínalo (en Next 14+ ya vienen por defecto)
  // experimental: { serverActions: true },
};

export default nextConfig;
